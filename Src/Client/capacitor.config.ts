import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.sprintretrospective',
  appName: 'SprintRetrospective',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
